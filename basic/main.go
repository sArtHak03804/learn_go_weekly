package main

import "fmt"

// Entry point of the application. Uncomment the required function to run it.

func main() {
	fmt.Println("`This line will come from main function file.`")
	// main()       //->the main file of this course/program.

	//hello()         //→ Start with "Hello, World!"
	//normal_values() //→ Work with constants and literals.
	//variables()      //→ Explore various types of variable declarations in Go, including constants and blank identifiers.
	//ifelse()     //→ work with if else in both way `Simple if-else` and `If-else if-else chain(ledder )`
	//calculator() //→ Practice combining all these concepts in a small project.

}
